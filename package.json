{
  "name": "nexedu_ts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src --ext ts,json --ignore dist --exec ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"No tests configured yet - passing for CI\" && exit 0",
    "test:coverage": "echo \"Coverage not configured yet\" && exit 0",
    "lint": "echo \"Linting not configured yet - passing for CI\" && exit 0",
    "lint:fix": "echo \"Lint fix not configured yet\" && exit 0",
    "format": "echo \"Formatting not configured yet\" && exit 0",
    "format:check": "echo \"Format check not configured yet\" && exit 0",
    "type-check": "tsc --noEmit",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:dev": "docker-compose --profile dev up -d",
    "docker:rebuild": "docker-compose up --build -d",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "eslint": "^9.31.0",
    "jest": "^30.0.4",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.11.0",
    "express": "^5.1.0",
    "prisma": "^6.11.0"
  }
}
